
#include<stdio.h>

void main()
{
    int a,arm=4,h,hi=5,d,de;
    for(a=1;a<=arm;a++) //Arm length
        {
            printf("L ");
            for(d=1;d<=32000;d++)
                for(de=1;de<=1200;de++);
        }
    printf("\n");
    for(h=1;h<=hi-2;h++) //Height
        {
            printf("L\n");
            for(d=1;d<=32000;d++)
                for(de=1;de<=1200;de++);
        }
    for(a=1;a<=arm;a++) //Arm length
        {
            printf("L ");
            for(d=1;d<=32000;d++)
                for(de=1;de<=1200;de++);
        }
}
